services:
  - type: web
    name: square-auctions-backend
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        value: "postgresql://TU_USUARIO:TU_CONTRASEÑA@TU_HOST:5432/subastas_db"